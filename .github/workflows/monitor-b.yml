name: letao-monitor-b

on:
  workflow_dispatch:   # 仅手动触发：单轮测试工具（长跑模式见 monitor.yml）

permissions:
  contents: write

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main   # 强制最新 main，避免 run 创建时固定的旧快照（github.sha）导致重复推送

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run monitor (single round)
        env:
          LETAO_TOKEN: ${{ secrets.LETAO_TOKEN }}
          STATE_DIR: ${{ github.workspace }}/monitor
        run: python monitor/letao_monitor.py

      - name: Send notification
        env:
          WXPUSHER_TOKEN: ${{ secrets.WXPUSHER_TOKEN }}
          WXPUSHER_UIDS: ${{ secrets.WXPUSHER_UIDS }}
          BARK_URL: ${{ secrets.BARK_URL }}
          WECOM_WEBHOOK: ${{ secrets.WECOM_WEBHOOK }}
          DING_WEBHOOK: ${{ secrets.DING_WEBHOOK }}
        run: python monitor/notify.py monitor/alerts.json
        # 注意：monitor-b 不 commit/push 状态文件——避免与主 run 并发修改 seen_items.json
        # 造成主 run 下一轮 git pull --rebase 冲突(128)。测试轮状态由主 run 负责维护。
